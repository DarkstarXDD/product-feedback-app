{
  "name": "product-feedback-app",
  "type": "module",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "lint": "cd backend && bun run lint",
    "type-check": "cd backend && bun run type-check",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "husky": "9.1.7",
    "prettier": "3.8.1"
  }
}
